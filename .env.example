# Database Configuration
# PostgreSQL connection string
DATABASE_URL="postgresql://username:password@localhost:5432/stream_sales?schema=public"

# JWT Configuration
# SECURITY: JWT_SECRET must be at least 32 characters long
# Generate a secure secret using: openssl rand -base64 32
# NEVER commit this secret to version control
# NEVER use the same secret in development and production
JWT_SECRET="CHANGE-THIS-TO-A-SECURE-RANDOM-STRING-AT-LEAST-32-CHARACTERS-LONG"

# JWT token expiration time (examples: 7d, 24h, 60m)
JWT_EXPIRES_IN="7d"

# CSRF Protection
# SECURITY: Generate using: openssl rand -base64 32
# Must be consistent across server instances
CSRF_SECRET="CHANGE-THIS-TO-A-SECURE-RANDOM-STRING-AT-LEAST-32-CHARACTERS-LONG"

# Environment
NODE_ENV="development"

# SECURITY NOTES:
# 1. Never commit .env file to version control (it's in .gitignore)
# 2. Use different secrets for development, staging, and production
# 3. Rotate secrets regularly (every 90 days recommended)
# 4. Store production secrets in a secure vault (e.g., AWS Secrets Manager, HashiCorp Vault)
# 5. Limit access to environment variables to only necessary personnel
# 6. Use strong, cryptographically random secrets
# 7. Keep this file updated when new environment variables are added
